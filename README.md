# rv-sparse-challenge
Simple C implementation for the rv-sparse LFX challenge. It converts a dense matrix to CSR format and performs sparse matrix-vector multiplication (SpMV) without using any dynamic memory allocation.
